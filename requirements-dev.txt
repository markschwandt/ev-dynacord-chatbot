# Dev-only dependencies for running the PDF processing pipeline locally.
# Not needed for the deployed Streamlit app (which only retrieves from data/vectorstore/).
# Install with: pip install -r requirements-dev.txt (in addition to requirements.txt)

PyMuPDF>=1.23.0
python-dotenv>=1.0.0
chromadb>=0.4.0
